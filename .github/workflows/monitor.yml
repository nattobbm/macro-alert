name: macro-monitor

on:
  schedule:
    # 2026-09-11 实测的真实延迟（不是文档说的 5-20 分钟，差一个数量级）：
    #   9-09  13:00 场 → 17:05 起跑     9-10  13:00 场 → 16:56     9-11  13:00 场 → 16:57
    #   9-11 那天 CPI 12:30Z 公布，网站 16:59Z 才反映，隔了 4.5 小时。
    #   盘中 :20/:50 那 16 个班次，9-11 只跑了 2 个，其余被 GitHub 丢弃。
    # 免费版 Actions 的定时器在整点/半点负载最高，这里把分钟挪开做缓解——
    # **只是缓解，不是解决**。要准点必须外部触发 workflow_dispatch（见 README）。
    # 美东 09:07 盘前（UTC 13:07，夏令时）
    - cron: '7 13 * * 1-5'
    # 美东 16:37 收盘后（UTC 20:37），覆盖当日拍卖结果
    - cron: '37 20 * * 1-5'
    # 盘中轻量刷价（只跑 --quotes-only，UTC 13-21点=美东9-17点）
    - cron: '13,43 13-20 * * 1-5'
  # 2026-09-10：手点一次 = 给她手机真发一条 TG。09-10 半夜那两条重复推送
  # 就是这么来的（03:46Z / 06:17Z 都不在 cron 上，是验证部署时手点的）。
  # 默认改成只跑不发；要真发得自己把 send 打开。
  workflow_dispatch:
    inputs:
      send:
        description: '真的推送到 Telegram？(默认只跑不发)'
        type: boolean
        default: false

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: monitor
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements.txt
      # 盘中的 :20/:50 场次只刷行情价(约15秒)，不跑规则不推TG；其余为完整流水线
      - name: Decide mode
        id: mode
        run: |
          if [ "${{ github.event.schedule }}" = "13,43 13-20 * * 1-5" ]; then
            echo "flag=--quotes-only" >> $GITHUB_OUTPUT
          else
            echo "flag=" >> $GITHUB_OUTPUT
          fi
      - name: Run monitor
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
          EIA_API_KEY: ${{ secrets.EIA_API_KEY }}
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          # 金十官方MCP。少了这行，Secret 加了也白加——fetchers/jin10.py 读的是
          # 环境变量，工作流不传就永远拿不到，油价会静默退回 yfinance 的坏K线口径
          JIN10_MCP_TOKEN: ${{ secrets.JIN10_MCP_TOKEN }}
        run: |
          DRY=""
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.send }}" != "true" ]; then
            DRY="--dry-run"
            echo "::notice::手动触发且未勾选 send → 只跑不推送"
          fi
          python monitor.py ${{ steps.mode.outputs.flag }} $DRY
      - name: Commit data
        run: |
          git config user.name  "macro-bot"
          git config user.email "bot@users.noreply.github.com"
          # 留痕：monitor.py 写的一行摘要（源健康/触发规则/延迟/日历条数/行情变化）
          MSG=$(cat data/_commit_msg.txt 2>/dev/null || echo "data: $(date -u +%FT%H:%M)")
          git add data/
          git diff --staged --quiet || git commit -m "$MSG"
          git push
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: site/package-lock.json
      - name: Build site (React)
        run: |
          cd site && npm ci && npm run build && cd ..
          mkdir -p _site
          cp -r site/dist/. _site/
          cp -r data _site/data
          cp -r knowledge _site/knowledge
          cp -r web _site/classic
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: monitor
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
